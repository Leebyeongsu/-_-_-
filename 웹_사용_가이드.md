# 스피드텔레콤_VisionFlow 사용 가이드

> 브라우저에서 바로 사용할 수 있습니다. 별도 설치나 실행이 필요 없습니다.

---

## 전체 화면 구성

화면은 총 **5개 카드**로 구성되어 있습니다.

```
[카드1] 이미지 업로드  →  [카드2] 변환 결과 / 다운로드  →  [카드3] 층호수 형태 변환

[카드4] 맵자료 원본 업로드  →  [카드5] 수정 및 변환 실행
```

---

## 기능 1: 이미지 → 기본 엑셀 변환 (카드1 → 카드2)

아파트 현황표 **이미지를 업로드**하면 AI가 자동으로 분석하여 엑셀 파일로 변환합니다.

### 사용 순서

**① 카드1 — 이미지 업로드**
- 카드1의 업로드 영역을 **클릭**하거나 이미지를 **드래그**하여 올려놓습니다
- 지원 형식: `jpg`, `png`, `gif` 등 이미지 파일
- 업로드 후 이미지 미리보기가 표시됩니다

**② 카드2 — 변환 시작**
- 카드2 하단의 **"변환 시작"** 버튼을 클릭합니다
- AI 분석 중... 표시와 함께 **10~30초** 대기합니다
- 완료되면 표 형태로 결과가 나타납니다

**③ 카드2 — 기본 엑셀 다운로드**
- **"기본 엑셀 다운로드"** 버튼을 클릭합니다
- `.xlsx` 파일이 다운로드 폴더에 저장됩니다
- 예시 파일명: `LG신주례1차_106동_1770693222524.xlsx`

### 이미지 색상 의미

| 원본 셀 색상 | 의미 |
|-------------|------|
| 노란색 | 모바일은 있으나 인터넷은 없는 고객 |
| 녹색 | Mega인터넷 또는 미동의인터넷 이용 고객 |
| 분홍색 | GiGA인터넷 이용 고객 |
| 흰색 | KT 상품 미이용 고객 |

> **팁:** 카메라 촬영보다 **화면 캡처 이미지**를 사용하면 인식률이 높습니다.

---

## 기능 2: 층호수 형태 변환 (카드3)

기능1에서 다운로드한 기본 엑셀을 **층호수 형태(2501, 1402...)** 로 자동 변환합니다.

### 사용 순서

**① 카드3 — 엑셀 파일 업로드**
- 카드3의 업로드 영역을 클릭합니다
- **기능1에서 다운로드한 기본 엑셀 파일**을 선택합니다
- 파일명과 크기가 표시되면 준비 완료

**② 카드3 — 층호수 형태로 변환**
- **"층호수 형태로 변환"** 버튼을 클릭합니다
- 1~3초 후 변환된 파일이 자동으로 다운로드됩니다
- 예시 파일명: `LG신주례1차_106동_1770693222524_변환.xlsx`

### 변환 결과 예시

| 층 | 01호 | | 02호 | | 03호 | |
|----|------|---|------|---|------|---|
| 25 | 2501 | KT | 2502 | | 2503 | M |
| 24 | 2401 | | 2402 | KT | 2403 | |

### 변환 규칙

| 원본 셀 색상 | 변환 텍스트 | 글자 색상 |
|-------------|-----------|---------|
| 녹색, 분홍색 | **KT** | 진한 빨간색 |
| 노란색 | **M** | 진한 녹색 |
| 흰색 | (빈칸) | — |

### 층호수 조합 방식

- 25층 + 01호 → **2501**
- 14층 + 02호 → **1402**
- 3층 + 10호 → **310**

> **주의:** 반드시 **기능1에서 다운로드한 파일**을 업로드하세요. 다른 엑셀 파일은 형식이 달라 변환이 안 될 수 있습니다.

---

## 기능 3: 동부산지사 아파트 맵자료 변환 (카드4 → 카드5)

아파트 동별 맵자료 원본 엑셀을 업로드하면 **동별 시트로 자동 분리**하고 텍스트를 정리합니다.

### 파일 준비 방법 (업로드 전)

> 맵자료는 이메일로 수신된 데이터를 아래 순서로 먼저 준비해야 합니다.

1. 이메일 본문에서 **엑셀 표 부분만 드래그하여 복사**합니다 (`Ctrl + C`)
2. 새로운 **Excel 파일을 열고** 붙여넣기 합니다 (`Ctrl + V`)
3. **다른 이름으로 저장** (`Ctrl + Shift + S`) → `.xlsx` 형식으로 저장합니다
4. 저장된 파일을 아래 순서로 업로드합니다

---

### 사용 순서

**① 카드4 — 원본 엑셀 업로드**
- 카드4의 업로드 영역을 클릭합니다
- **위에서 준비한 엑셀 파일**을 선택합니다
  - 조건: **1~3행에 동 이름(예: 101동, 102동)이 있어야** 합니다
- 파일명과 크기가 표시되면 준비 완료

**② 카드5 — 수정 및 변환 실행**
- **"수정 및 변환"** 버튼을 클릭합니다
- 1~5초 후 변환된 파일이 자동으로 다운로드됩니다
- 예시 파일명: `현황표_107동_원본_동별시트.xlsx`

### 변환 결과

다운로드된 파일을 Excel에서 열면:

- **"원본"** 시트: 업로드한 원본 데이터 보존
- **동별 시트** (101동, 102동...): 각 동의 데이터가 정리되어 분리됨

### 자동 처리 내용

| 항목 | 처리 내용 |
|------|---------|
| 동별 분리 | 1~3행의 동 이름 기준으로 시트 자동 생성 |
| 데이터 이동 | 호수 아래 데이터 → 호수 옆 열로 이동 후 원본 행 삭제 |
| 호수 열 너비 | 5.00 고정 |
| 행 높이 | 25 고정 |
| 테두리 | 모든 셀 얇은 실선 자동 적용 |

### 텍스트 자동 변환 규칙

| 원본 텍스트 | 변환 결과 | 글자 색상 |
|-----------|---------|---------|
| `M` | **M** | 진한 녹색 |
| `M (미동의)` | **KT** | 진한 빨간색 |
| `I M (미동의)` | **KT** | 진한 빨간색 |
| `I T M (미동의)` | **KT** | 진한 빨간색 |
| `I T (미동의)` | **KT** | 진한 빨간색 |
| `T M (미동의)` | **KT** | 진한 빨간색 |
| `I (미동의)` | **KT** | 진한 빨간색 |
| `I M` | **KT** | 진한 빨간색 |
| `I T M` | **KT** | 진한 빨간색 |
| `T M` | **KT** | 진한 빨간색 |
| `I T` | **KT** | 진한 빨간색 |
| `I` | **KT** | 진한 빨간색 |
| `T (미동의)` | (빈 셀) | — |

> 텍스트 안의 공백은 자동으로 제거 후 비교합니다. `I M`과 `IM` 모두 동일하게 처리됩니다.

---

## 자주 묻는 질문 (FAQ)

### "변환 시작"을 눌렀는데 오류가 나와요
→ 잠시 후 다시 시도해 주세요. 서버가 일시적으로 응답이 늦을 수 있습니다.
→ 계속 오류가 나면 **이미지가 선명한지** 확인하세요.

### 이미지 변환 결과가 정확하지 않아요
→ 흐린 사진이나 너무 작은 이미지는 인식률이 떨어집니다.
→ 가능하면 **화면 캡처 이미지**를 사용하세요.

### 기능2 변환이 실패해요
→ 반드시 **기능1에서 다운로드한 기본 엑셀 파일**을 업로드하세요.
→ 직접 만든 파일이나 다른 형식은 변환이 되지 않습니다.

### 기능3 변환 후 동 시트가 생성되지 않아요
→ 원본 엑셀의 **1~3행에 "101동", "102동"** 과 같은 동 이름이 있어야 합니다.
→ 동 이름 형식이 다르면 시트가 생성되지 않습니다.

---

> 문제가 지속되면 담당자에게 문의하세요.
